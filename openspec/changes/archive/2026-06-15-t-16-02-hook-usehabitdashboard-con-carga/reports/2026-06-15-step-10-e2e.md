# Informe Paso 10 - Pruebas E2E (Playwright MCP)

- Fecha: 2026-06-15
- Cambio: t-16-02-hook-usehabitdashboard-con-carga
- Agente: Claude (opsx-apply)
- URL Frontend: http://localhost:5173

## Entorno
- Frontend: PASS — `npm run dev:web` (Vite v6.4.2, puerto 5173)
- Backend: PASS — `npm run dev:api` (puerto 3001)
- Base de datos: PASS — `npm run db:up` (contenedor `conrutina-db`, datos de seed)

## Escenarios ejecutados

### Carga del dashboard con hábitos reales (US-16 Sc. carga inicial)
1. `browser_navigate` → http://localhost:5173: PASS
2. `GET /api/weeks/current` → 200: PASS
3. El dashboard muestra los 4 hábitos del seed (Correr, Meditar, Leer, Test curl) con `completionStatus`/`streak`/`stats` derivados de la respuesta: PASS

### Toggle de celda + persistencia (US-16 Sc. 2 y 3)
1. Click en la celda de Lunes del hábito "Correr" (estado `pending`): PASS — la celda cambia a `completed` de forma inmediata (optimistic update)
2. `PATCH /api/habit-entries/45` con `{status:'completed'}` → 200: PASS
3. Recarga de página (`browser_navigate` de nuevo): PASS — la celda sigue `completed`, `streak` muestra "🔥 1 días", "Esta semana" pasa a `+10` y "Progreso de hoy" a `25%`

### Alta de hábito (US-16 Sc. 1)
1. Click en "+ Nuevo hábito", seleccionar emoji 🌟, nombre "Hábito E2E", valores por defecto de puntos/penalización: PASS
2. Click en "Añadir hábito": PASS — `POST /api/habits` → 201
3. Recarga automática de la semana activa: `GET /api/weeks/current` → 200: PASS

### Baja de hábito (US-16 Sc. 4)
1. Click en "×" del hábito "Test curl": PASS — el hábito desaparece de `habits` de forma optimista (inmediata, antes de la respuesta de red)
2. `DELETE /api/habits/4` → 204: PASS

### Navegación de semana (US-16 Sc. 5)
1. Click en "‹" (semana anterior): PASS — `GET /api/weeks?offset=-1` → 200 con `week.isLocked=true`
2. El hook expone `isWeekLocked=true`/`isCurrentWeek=false`: PASS — las celdas se renderizan como `<div>` de solo lectura (no `<button>`) con iconos `Check`/`X`, y el botón "×" de eliminar desaparece de cada fila (consecuencia de `isReadOnly` en `HabitRow`)
3. Click en "›" (volver a semana actual): PASS — `GET /api/weeks?offset=0` → 200 con `week.isLocked=false`, `isCurrentWeek=true`

## Persistencia de datos
- Toggle de celda: PASS — persiste tras recargar (ver arriba)
- Estado BD alineado con UI: PASS

## Observaciones
- **[Fuera de alcance, backend]** El hábito "Hábito E2E" creado en el escenario de alta no aparece en la lista tras la recarga de `fetchCurrentWeek()`, porque el backend (`getCurrentWeekResponse`/`mapWeekToApiResponse`, sin cambios en este ticket) no añade un `WeekHabit` snapshot a la semana activa ya existente para hábitos creados después de la creación de dicha semana. `POST /api/habits` (201) y la recarga (`GET /api/weeks/current` 200) se ejecutan correctamente según lo especificado en `tasks.md` 5.1; el hábito sí queda persistido (`GET /api/habits` lo devuelve con `id=6`). Documentado como limitación conocida para un ticket futuro de backend.
- No existe un badge textual "Semana bloqueada" en `presentation/` (no se introdujo en este ticket ni en anteriores); el comportamiento de bloqueo se verifica mediante `isWeekLocked` (celdas de solo lectura, sin botón "×"), que es el contrato que expone el hook según `design.md`/`specs/`.
- `browser_console_messages` no reportó errores nuevos relacionados con este change.

## Resultado
- Estado del Paso 10: PASS
- Todos los escenarios pasaron: Sí
- Entorno restaurado: pendiente de detener `npm run dev` / `npm run dev:api` al finalizar la sesión
- Problemas bloqueantes: ninguno
