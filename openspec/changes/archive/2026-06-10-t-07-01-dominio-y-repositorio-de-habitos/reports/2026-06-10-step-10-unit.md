# Informe Paso 10 - Tests unitarios

- Fecha: 2026-06-10
- Cambio: t-07-01-dominio-y-repositorio-de-habitos
- Agente: Auto

## Tipo de verificación
- [x] Tests unitarios
- [ ] Verificación manual de la aplicación

## Pasos de verificación realizados

### Tests unitarios createHabit / getActiveHabits
- `npm test -- backend/src/application/createHabit.test.ts backend/src/application/getActiveHabits.test.ts`: **PASS** (8 tests)
- Suite backend completa `npm test`: **PASS** (6 archivos, 27 tests)

### Casos cubiertos — createHabit
- Happy path: input válido → `repo.create` con `userId` y campos → retorna hábito (US-07 S1)
- Edge: `name` vacío/ausente → `ValidationError`, `create` no invocado (US-07 S3)
- Edge: `pointsPerDay: 0` → `ValidationError` (US-07 S4)
- Edge: sin `emoji` → `ValidationError` (US-07 S5)
- Edge: `penalty: -1` → `ValidationError`

### Casos cubiertos — getActiveHabits
- Happy path: repo devuelve 2 hábitos activos → mismo array al caller (US-07 S2)
- Edge: repo devuelve `[]` → use case retorna `[]`

## Comandos ejecutados
```
npm test -- backend/src/application/createHabit.test.ts backend/src/application/getActiveHabits.test.ts
npm test
```

## Observaciones
Mock de `HabitRepository` con Vitest (`vi.fn()`); sin Prisma ni BD real.

## Resultado
- Estado del Paso 10: **PASS**
- Problemas bloqueantes: ninguno
