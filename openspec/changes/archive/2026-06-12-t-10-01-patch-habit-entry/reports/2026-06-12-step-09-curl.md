# Informe Paso 9 - Pruebas manuales de endpoints

- Fecha: 2026-06-12
- Cambio: t-10-01-patch-habit-entry
- Agente: Auto
- URL Backend: http://localhost:3001

## Preparación

```bash
npm run db:up
npm run dev:api
```

`entryId` obtenido de `GET /api/weeks/current` → `habits[0].entries[0].id` = **22**

> **Nota Windows/PowerShell:** `curl -d "{\"status\":...}"` falla por escape de comillas. Se usó `node fetch` equivalente; en bash los comandos curl del ticket funcionan tal cual.

## Endpoints probados

### PATCH /api/habit-entries/22 — happy path (US-10 S1)

```bash
curl -s -X PATCH http://localhost:3001/api/habit-entries/22 \
  -H "Content-Type: application/json" \
  -d '{"status":"completed"}'
```

**Respuesta:**
```json
{"id":22,"status":"completed","updatedAt":"2026-06-12T14:18:39.773Z"}
```

**Código de estado:** 200 OK  
**Verificación:** PASS

### PATCH /api/habit-entries/22 — ciclo completed → failed (US-10 S3)

```bash
curl -s -X PATCH http://localhost:3001/api/habit-entries/22 \
  -H "Content-Type: application/json" \
  -d '{"status":"failed"}'
```

**Respuesta:**
```json
{"id":22,"status":"failed","updatedAt":"2026-06-12T14:18:53.616Z"}
```

**Código de estado:** 200 OK  
**Verificación:** PASS — `updatedAt` cambió entre PATCH consecutivos

## Pruebas de casos de error

### Status inválido (US-10 S4)

```bash
curl -s -X PATCH http://localhost:3001/api/habit-entries/22 \
  -H "Content-Type: application/json" \
  -d '{"status":"done"}'
```

**Respuesta:**
```json
{
  "code": "VALIDATION_ERROR",
  "message": "Datos inválidos",
  "details": [{ "field": "status", "message": "Debe ser pending, completed o failed" }]
}
```

**Código de estado:** 400  
**Verificación:** PASS

### Entry inexistente

```bash
curl -s -X PATCH http://localhost:3001/api/habit-entries/99999 \
  -H "Content-Type: application/json" \
  -d '{"status":"completed"}'
```

**Respuesta:**
```json
{"code":"HABIT_ENTRY_NOT_FOUND","message":"Entrada de hábito no encontrada"}
```

**Código de estado:** 404  
**Verificación:** PASS

### Id no numérico

```bash
curl -s -X PATCH http://localhost:3001/api/habit-entries/abc \
  -H "Content-Type: application/json" \
  -d '{"status":"completed"}'
```

**Código de estado:** 404 — `HABIT_ENTRY_NOT_FOUND`  
**Verificación:** PASS

### Semana bloqueada (US-10 S2) — N/A en seed actual

```bash
curl -s "http://localhost:3001/api/weeks?offset=-1"
```

**Respuesta:** 404 `WEEK_NOT_FOUND` — no existe semana histórica bloqueada en BD tras seed.

**Verificación:** N/A — escenario 409 `WEEK_LOCKED` cubierto por tests unitarios (`updateHabitEntry.test.ts`, `createApp.test.ts`). Para curl manual haría falta semana previa con `isLocked=true` en seed o forzar transición de semana.

## Gestión del estado de la base de datos

- Estado previo: entry 22 `pending`
- Estado posterior a pruebas: entry 22 `failed`
- Estado restaurado: **Sí** — `npm run db:seed`

## Observaciones

- Persistencia real verificada: `updatedAt` ISO y `status` en PostgreSQL.
- Seed solo crea semana activa (`isLocked=false`); 409 manual documentado como N/A.

## Resultado

- Estado del Paso 9: **PASS**
- Todos los endpoints probados: Sí (409 vía tests unitarios)
- Base de datos restaurada: Sí
- Problemas bloqueantes: ninguno
