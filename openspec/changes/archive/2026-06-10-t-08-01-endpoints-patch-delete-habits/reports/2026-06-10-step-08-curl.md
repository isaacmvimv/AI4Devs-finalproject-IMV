# Informe Paso 8 - Pruebas manuales de endpoints

- Fecha: 2026-06-10
- Cambio: t-08-01-endpoints-patch-delete-habits
- Agente: Auto
- URL Backend: http://localhost:3001

## Endpoints probados

### GET /api/habits
```bash
curl.exe -s http://localhost:3001/api/habits
```

**Respuesta:** Lista con hábitos seed (ids 1–4).
**Código de estado:** 200 OK
**Verificación:** PASS — obtención de id para PATCH

### PATCH /api/habits/1
```bash
curl.exe -s -w "\nHTTP_STATUS:%{http_code}" -X PATCH http://localhost:3001/api/habits/1 \
  -H "Content-Type: application/json" --data-binary "@tmp-patch-body.json"
# body: {"pointsPerDay":15}
```

**Respuesta:**
```json
{"id":1,"userId":1,"emoji":"🏃","name":"Correr","pointsPerDay":15,"penalty":5,"isActive":true,"createdAt":"2026-05-30T13:31:26.634Z"}
```

**Código de estado:** 200 OK
**Verificación:** PASS — `pointsPerDay` actualizado a 15

### PATCH /api/habits/99999 (error)
```bash
curl.exe -s -w "\nHTTP_STATUS:%{http_code}" -X PATCH http://localhost:3001/api/habits/99999 \
  -H "Content-Type: application/json" --data-binary "@tmp-patch-body.json"
```

**Respuesta:**
```json
{"code":"HABIT_NOT_FOUND","message":"Hábito no encontrado"}
```

**Código de estado:** 404 Not Found
**Verificación:** PASS

### POST /api/habits (hábito de prueba para DELETE)
```bash
curl.exe -s -w "\nHTTP_STATUS:%{http_code}" -X POST http://localhost:3001/api/habits \
  -H "Content-Type: application/json" --data-binary "@tmp-post-habit.json"
```

**Respuesta:** `{"id":5,...,"name":"Test DELETE curl",...}`
**Código de estado:** 201 Created
**Verificación:** PASS

### DELETE /api/habits/5
```bash
curl.exe -s -w "\nHTTP_STATUS:%{http_code}" -X DELETE http://localhost:3001/api/habits/5
```

**Respuesta:** (vacía)
**Código de estado:** 204 No Content
**Verificación:** PASS — GET /api/habits ya no incluye id 5

### WeekHabit snapshots tras PATCH (invariante US-08 S4)
Consulta SQL vía `docker exec conrutina-db psql`:

```
 id | habitId | snapshotName | snapshotPoints | snapshotPenalty
----+---------+--------------+----------------+-----------------
  1 |       1 | Correr       |             10 |               5
  4 |       1 | Correr       |             10 |               5
```

Tras PATCH de hábito 1 a `pointsPerDay: 15`, los snapshots siguen en `snapshotPoints: 10`.
**Verificación:** PASS — snapshots WeekHabit intactos

## Gestión del estado de la base de datos
- Estado previo a pruebas: seed con hábitos 1–3 (+ residuo id 4 de prueba anterior)
- Estado posterior a pruebas: hábito 5 desactivado; hábito 1 con pointsPerDay=15
- Estado restaurado: Sí
- Acciones de restauración: `npm run db:seed`

## Observaciones
- En PowerShell, `curl.exe -d "{\"pointsPerDay\":15}"` falla por escape de JSON; se usaron archivos temporales `--data-binary @file.json`.

## Resultado
- Estado del Paso 8: PASS
- Todos los endpoints probados: Sí
- Base de datos restaurada: Sí
- Problemas bloqueantes: ninguno
