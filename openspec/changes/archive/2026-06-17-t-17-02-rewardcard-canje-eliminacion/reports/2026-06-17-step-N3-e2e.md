# E2E Report — T-17-02 · Step N+3 Playwright

**Date:** 2026-06-17
**Change:** t-17-02-rewardcard-canje-eliminacion
**URL:** http://localhost:5173

---

## Condiciones

- Frontend activo: `npm run dev` (Vite)
- Backend: no activo (sin `npm run dev:api`)
- Puntos del perfil: 0 (carga desde API fallida → stats vacías)

## Resultados

| Escenario | Descripción | Resultado |
|-----------|-------------|-----------|
| SC1 | Saldo insuficiente → botones "Faltan X pts" deshabilitados | PASS |
| SC2 | Canje exitoso (spinner → toast → "¡Canjeada!") | N/A — requiere backend activo con puntos suficientes |
| SC5 | Eliminación: clic × → tarjeta desaparece del catálogo | PASS |

## SC1 — Saldo insuficiente

Con `currentPoints=0`, todas las tarjetas muestran:
- Botón deshabilitado con texto "Faltan X pts" (X = cost)
- Lock icon visible

Snapshot: `e2e-sc1-saldo-insuficiente.png`

## SC5 — Eliminación

Se hizo clic en × sobre "Película favorita". La tarjeta desapareció inmediatamente del catálogo. Las 3 tarjetas restantes permanecen sin alteración.

Snapshot: `e2e-sc5-eliminacion.png`

## SC2/SC4 — Canje (requiere backend)

No verificable sin `npm run dev:api` y puntos disponibles. Los tests unitarios cubren exhaustivamente estos escenarios:
- `RewardCard.test.tsx SC3` — canje exitoso → `onRedeemSuccess(80)` + `toast.success`
- `RewardCard.test.tsx SC4` — error 422 → `toast.error` + `onRedeemSuccess` no llamado

---

**Resultado global:** PASS (SC verificables en E2E aprobados; SC2/SC4 cubiertos por tests unitarios)
