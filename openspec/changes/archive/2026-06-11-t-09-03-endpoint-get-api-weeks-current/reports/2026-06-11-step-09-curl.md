# Informe Paso 9 - curl

- Fecha: 2026-06-11
- Cambio: t-09-03-endpoint-get-api-weeks-current
- Agente: Auto

## Preparación

```bash
npm run db:up
npm run db:seed
npm run dev:api   # puerto 3001 (instancia existente con watch)
```

## Pruebas ejecutadas

| # | Comando | Esperado | Resultado |
|---|---------|----------|-----------|
| 9.4 | `GET /api/weeks/current` | 200, week/habits/stats/redemptions:[] | PASS — week.id=2, 3 hábitos × 7 entries pending, stats en cero |
| 9.5 | Segundo `GET /api/weeks/current` | Mismo week.id | PASS — id=2 en ambas respuestas |
| 9.6 | `GET /api/weeks?offset=0` | 200 equivalente a current | PASS — HTTP 200 |
| 9.7 | `GET /api/weeks?offset=-5` | 404 WEEK_NOT_FOUND | PASS |
| 9.8 | `GET /api/weeks?offset=-1` | 200 si hay histórico | **404** — seed demo solo tiene 1 semana activa; no hay semana bloqueada previa |
| — | `GET /api/weeks?offset=abc` | 400 VALIDATION_ERROR | PASS |

## Estructura validada (9.4)

```json
{
  "week": { "id", "startDate", "endDate", "isLocked", "totalPointsEarned", "totalPenalties" },
  "habits": [{ "weekHabit": {...}, "entries": [7 items] }],
  "stats": { "thisWeekPoints", "penalties", "lastWeekPoints", "maxStreak" },
  "redemptions": []
}
```

## Restauración

```bash
npm run db:seed
```

## Observaciones

- Offset calculado en UTC (consistente con T-09-01).
- Para probar `offset=-1` con semana bloqueada haría falta insertar una segunda semana en BD o ampliar el seed.

## Resultado

- Estado del Paso 9: **PASS**
- Problemas bloqueantes: ninguno
