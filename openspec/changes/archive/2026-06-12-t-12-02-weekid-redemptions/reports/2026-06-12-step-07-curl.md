# Informe Paso 7 - Pruebas manuales de endpoints (curl)

- Fecha: 2026-06-12
- Cambio: t-12-02-weekid-redemptions
- Agente: Auto
- URL Backend: http://localhost:3001

## Preparación

- `npm run db:up` — PostgreSQL activo: **PASS**
- `npm run dev:api` — servidor en puerto 3001: **PASS**
- `weekId` activo: **2** (`GET /api/weeks/current`)

## Endpoints probados

### GET /api/weeks/current (7.3)

```bash
curl.exe -s http://localhost:3001/api/weeks/current
```

**Verificación:** PASS — `week.id = 2`

### GET /api/rewards (7.4)

```bash
curl.exe -s http://localhost:3001/api/rewards
```

**Verificación:** PASS — recompensas activas id 1 (cost 50), id 2 (cost 80)

### POST /api/weeks/2/redemptions — happy path (7.5, US-12 S1)

Tras completar entradas de hábito para acumular saldo, se creó recompensa auxiliar id=4 (cost 30) y:

```bash
curl.exe -s -X POST http://localhost:3001/api/weeks/2/redemptions \
  -H "Content-Type: application/json" \
  -d '{"rewardId":4}'
```

**Respuesta:**

```json
{"id":5,"weekId":2,"rewardId":4,"pointsSpent":30,"redeemedAt":"2026-06-12T20:43:28.972Z"}
```

**Código de estado:** 201 Created  
**Verificación:** PASS — campos DoD presentes (`id`, `weekId`, `rewardId`, `pointsSpent`, `redeemedAt` ISO)

### POST /api/weeks/2/redemptions — puntos insuficientes (7.6, US-12 S2)

```bash
curl.exe -s -X POST http://localhost:3001/api/weeks/2/redemptions \
  -H "Content-Type: application/json" \
  -d '{"rewardId":2}'
```

**Respuesta:**

```json
{"code":"INSUFFICIENT_POINTS","message":"Puntos insuficientes","details":{"available":16,"required":80}}
```

**Código de estado:** 422 Unprocessable Entity  
**Verificación:** PASS — `details.available` y `details.required` presentes

### POST /api/weeks/99999/redemptions — semana inexistente (7.7)

```bash
curl.exe -s -X POST http://localhost:3001/api/weeks/99999/redemptions \
  -H "Content-Type: application/json" \
  -d '{"rewardId":1}'
```

**Respuesta:** `{"code":"WEEK_NOT_FOUND","message":"Semana no encontrada"}`

**Código de estado:** 404 Not Found  
**Verificación:** PASS

### POST /api/weeks/2/redemptions — recompensa inexistente (7.8)

```bash
curl.exe -s -X POST http://localhost:3001/api/weeks/2/redemptions \
  -H "Content-Type: application/json" \
  -d '{"rewardId":99999}'
```

**Respuesta:** `{"code":"REWARD_NOT_FOUND","message":"Recompensa no encontrada"}`

**Código de estado:** 404 Not Found  
**Verificación:** PASS

## Gestión del estado de la base de datos

- Estado previo a pruebas: seed con usuario id=1, semana activa y recompensas
- Estado posterior a pruebas: canjes y entradas completadas durante pruebas
- Estado restaurado: Sí — `npm run db:seed` (7.9)

## Observaciones

- En PowerShell, `curl` es alias de `Invoke-WebRequest`; se usó `curl.exe` con `-d @archivo.json` para evitar problemas de escape JSON.
- Para el happy path 201 fue necesario acumular puntos completando `HabitEntry` vía `PATCH /api/habit-entries/:id` (el seed inicia con entries `pending`).

## Resultado

- Estado del Paso 7: **PASS**
- Todos los endpoints probados: **Sí**
- Base de datos restaurada: **Sí**
- Problemas bloqueantes: ninguno
