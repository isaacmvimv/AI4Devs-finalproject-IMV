# Informe Paso 6 - Pruebas manuales de endpoints

- Fecha: 2026-06-10
- Cambio: t-06-02-endpoint-get-api-profile
- Agente: Auto
- URL Backend: http://localhost:3001

## Endpoints probados

### GET /api/profile
```powershell
Invoke-WebRequest -Uri http://localhost:3001/api/profile -UseBasicParsing
```

**Respuesta:**
```json
{"id":1,"name":"Demo User","email":"demo@ConRutina.app","avatarUrl":null}
```

**Código de estado:** 200 OK
**Verificación:** PASS — datos del seed (`id=1`, `name`, `email`, `avatarUrl`)

## Pruebas de casos de error

### Usuario no encontrado (404)
Cobertura vía test supertest con mock (`createApp.test.ts`): mock lanza `NotFoundError` → 404 `{ code: "USER_NOT_FOUND", message: "Usuario no encontrado" }`. No se eliminó el usuario del seed para evitar alterar datos de desarrollo.

**Verificación:** PASS (mock test)

## Gestión del estado de la base de datos
- Estado previo a pruebas: PostgreSQL levantado con `npm run db:up`
- Estado posterior a pruebas: sin cambios (solo lectura)
- Estado restaurado: N/A
- Acciones de restauración: ninguna

## Observaciones
En Windows, `curl -s` quedó colgado; se usó `Invoke-WebRequest` con equivalente funcional.

## Resultado
- Estado del Paso 6: **PASS**
- Todos los endpoints probados: Sí
- Base de datos restaurada: N/A
- Problemas bloqueantes: ninguno
