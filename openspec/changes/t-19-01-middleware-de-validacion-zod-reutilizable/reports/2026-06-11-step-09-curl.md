# Informe Paso 9 - Pruebas manuales de endpoints

- Fecha: 2026-06-11
- Cambio: t-19-01-middleware-de-validacion-zod-reutilizable
- Agente: Auto
- URL Backend: http://localhost:3001

## Preparación

```bash
npm run db:up
npm run dev:api
```

## Endpoints probados

### POST /api/habits — body incompleto (US-19 S2)

```bash
curl.exe -s -w "\nHTTP_STATUS:%{http_code}" -X POST http://localhost:3001/api/habits \
  -H "Content-Type: application/json" \
  --data-binary "@tmp-curl-post-incomplete.json"
# Body: {"emoji":"🏃"}
```

**Respuesta:**
```json
{
  "code": "VALIDATION_ERROR",
  "message": "Datos inválidos",
  "details": [
    { "field": "name", "message": "Invalid input: expected string, received undefined" },
    { "field": "pointsPerDay", "message": "Invalid input: expected number, received undefined" },
    { "field": "penalty", "message": "Invalid input: expected number, received undefined" }
  ]
}
```

**Código de estado:** 400
**Verificación:** PASS — `details` incluye `name` y `pointsPerDay`

### POST /api/habits — name vacío

```bash
curl.exe -s -w "\nHTTP_STATUS:%{http_code}" -X POST http://localhost:3001/api/habits \
  -H "Content-Type: application/json" \
  --data-binary "@tmp-curl-post-empty-name.json"
# Body: {"emoji":"🏃","name":"","pointsPerDay":10,"penalty":5}
```

**Respuesta:**
```json
{
  "code": "VALIDATION_ERROR",
  "message": "Datos inválidos",
  "details": [{ "field": "name", "message": "El nombre es obligatorio" }]
}
```

**Código de estado:** 400
**Verificación:** PASS — detail `field: name`

### PATCH /api/habits/1 — body vacío

```bash
curl.exe -s -X PATCH http://localhost:3001/api/habits/1 \
  -H "Content-Type: application/json" -d "{}"
```

**Respuesta:**
```json
{
  "code": "VALIDATION_ERROR",
  "message": "Datos inválidos",
  "details": [
    { "field": "input", "message": "Debe proporcionar al menos un campo para actualizar" }
  ]
}
```

**Código de estado:** 400
**Verificación:** PASS — detail `field: input`

## Gestión del estado de la base de datos

- Estado previo a pruebas: seed existente tras `db:up`
- Mutaciones de prueba: ninguna (solo validación, sin creates)
- Estado restaurado: Sí — `npm run db:seed`

## Observaciones

En PowerShell, el escape de JSON inline con `curl -d "{\"...\"}"` produce JSON malformado; se usaron ficheros temporales con `--data-binary @file` (eliminados tras la prueba).

## Resultado

- Estado del Paso 9: PASS
- Todos los endpoints probados: Sí
- Base de datos restaurada: Sí
- Problemas bloqueantes: ninguno
